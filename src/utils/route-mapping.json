//前后端对应关系说明

// {
//   "path": "path", //访问url，必填项
//   "name":"name",  // 首字母大写，一定要与vue文件的name对应起来，用于noKeepAlive缓存控制（该项特别重要）
//   "component": "component", //组件位置，必填项
//   "redirect": "redirect", //是否跳转，可选项
//   "meta": {
//     "title": "title", //页面标题，必填项
//     "remixIcon": "icon", //菜单图标，可选项
//     "affix": "affix", //当前路由是否固定多标签页，（默认值：true），必填项
//     "hidden": "hidden", //是否显示在菜单中显示路由（默认值：false），必填项
//     "target": "target", //是否连接跳转，可选项
//     "alwaysShow": "alwaysShow", //当只有一级子路由时是否显示父路由是否显示在菜单中显示路由（默认值：false），必填项
//     "tagHidden": "tagHidden" // 当前路由是否不显示多标签页（默认值：false），可选项
//     ,"keepAlive":"keepAlive" //当前路由是否不缓存（默认值：false），可选项
//     // ,"roles":"roles" //["admin"]，所属角色,必填项
//     ,"ability":"ability" //["add","delete","edit","search","export","import","custom"]，必填项
//   },
//   "children": "children" //是否有子节点[],可选项
// }
{
  "path": "/a/b", //访问url，必填项
  "name": "welcome", // 首字母大写，一定要与vue文件的name对应起来，用于noKeepAlive缓存控制（该项特别重要）
  "component": "view/sdfsdf/abc", //组件位置，必填项
  "redirect": "/ab", //是否跳转，可选项
  "meta": {
    "title": "测试组件", //页面标题，必填项
    "icon": "sskdf", //菜单图标，可选项
    "affix": true, //当前路由是否固定多标签页，（默认值：true），必填项
    "hidden": false, //是否显示在菜单中显示路由（默认值：false），必填项
    "target": "target", //是否连接跳转，可选项
    "alwaysShow": false, //当只有一级子路由时是否显示父路由是否显示在菜单中显示路由（默认值：false），必填项
    "tagHidden": false, // 当前路由是否不显示多标签页（默认值：false），可选项
    "keepAlive": false, //当前路由是否不缓存（默认值：false），可选项
    "ability": ["add", "delete", "edit"] //["add","delete","edit","search","export","import","upload",audit,"custom"]，按钮权限值，必填项
  },
  "children": [] //是否有子节点[],可选项
}
